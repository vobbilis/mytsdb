apiVersion: v1
kind: ConfigMap
metadata:
  name: otel-collector-config
  namespace: mytsdb
  labels:
    app: otel-collector
data:
  otel-collector-config.yaml: |
    receivers:
      otlp:
        protocols:
          grpc:
            endpoint: 0.0.0.0:4317
          http:
            endpoint: 0.0.0.0:4318
    
    processors:
      batch:
        timeout: 10s
        send_batch_size: 1024
      
      memory_limiter:
        check_interval: 1s
        limit_mib: 512
    
    exporters:
      # Export OTLP/gRPC directly into MyTSDB (validates the OTEL gRPC ingestion path).
      otlp:
        endpoint: "mytsdb.mytsdb.svc.cluster.local:4317"
        tls:
          insecure: true
      
      logging:
        loglevel: debug
    
    service:
      pipelines:
        metrics:
          receivers: [otlp]
          processors: [memory_limiter, batch]
          exporters: [otlp, logging]
