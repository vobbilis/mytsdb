# TSDB Storage Architecture

## 🗄️ **Storage System Overview**

The TSDB storage architecture implements a multi-tier storage system designed for high performance, efficient compression, and optimal data lifecycle management. The system uses a block-based storage model with intelligent caching and compression strategies.

## 🏗️ **Multi-Tier Storage Architecture**

### **Storage Tiers**
```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              STORAGE HIERARCHY                                  │
└─────────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────┐
│                              TIER 1: HOT STORAGE                               │
├─────────────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐           │
│  │  Working    │  │  Object     │  │  Sharded    │  │  Sync       │           │
│  │  Set Cache  │  │  Pooling    │  │  Index/WAL  │  │  Maps       │           │
│  │  (L1)       │  │             │  │  Manager    │  │             │           │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘           │
│                                                                                 │
│  • In-Memory Storage (RAM)                                                     │
│  • Fastest Access                                                              │
│  • LRU Eviction Policy                                                         │
│  • High Cache Hit Ratio goal                                                   │
└─────────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              TIER 2: WARM STORAGE                              │
├─────────────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐           │
│  │  Memory     │  │  Predictive │  │  Adaptive   │  │  Background │           │
│  │  Mapped     │  │   Cache     │  │ Compressor  │  │  Processor  │           │
│  │  Cache      │  │             │  │             │  │             │           │
│  │  (L2)       │  │             │  │             │  │             │           │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘           │
│                                                                                 │
│  • Memory-Mapped Files                                                         │
│  • Medium Access Speed (1-5ms)                                                 │
│  • Pattern-Based Prefetching                                                   │
│  • Compressed Data Storage                                                     │
└─────────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              TIER 3: COLD STORAGE                              │
├─────────────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐           │
│  │  Block      │  │  Series     │  │  File       │  │  Index      │           │
│  │  Manager    │  │  Manager    │  │  Manager    │  │  Manager    │           │
│  │             │  │             │  │             │  │             │           │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘           │
│                                                                                 │
│  • Disk-Based Storage (SSD/HDD)                                                │
│  • Format: **Apache Parquet** (Columnar)                                       │
│  • Compression: **ZSTD** + **Dictionary Encoding**                             │
│  • Read-Optimized (OLAP)                                                       │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### **Apache Parquet Cold Storage**

The Cold Tier is built on **Apache Parquet**, an open-source columnar storage format. This design choice aligns MyTSDB with modern "Lakehouse" architectures (similar to InfluxDB IOx), offering significant advantages over proprietary formats:

1.  **Superior Compression**:
    -   **Columnar Layout**: Values of the same type are stored contiguously, enabling highly effective compression (RLE, Bit-Packing).
    -   **Dictionary Encoding**: High-cardinality string labels (e.g., `pod_name`, `request_id`) are dictionary-encoded, reducing storage size by up to 90%.
    -   **ZSTD**: We use ZSTD compression for an optimal balance of compression ratio and decompression speed.

2.  **Schema Evolution**:
    -   Parquet supports sparse columns and schema merging.
    -   Adding new dimensions (e.g., `region=us-east`) to existing metrics does **not** require rewriting old files or creating new series IDs.
    -   This solves the "Cardinality Curse" common in LSM-based TSDBs.

3.  **Interoperability**:
    -   Data stored in the Cold Tier is not locked in a proprietary format.
    -   Files can be directly queried by analytical tools like **DuckDB**, **Pandas**, **Spark**, or **Presto** for ad-hoc OLAP analysis.

4.  **Hybrid Query Engine**:
    -   The query engine transparently merges results from the **Hot Tier** (Memory) and **Cold Tier** (Parquet).
    -   Recent data is available immediately (low latency), while historical data is fetched efficiently from disk.

## 📦 **Block Management System**

### **Block Structure**
```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              BLOCK STRUCTURE                                    │
└─────────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────┐
│                              BLOCK HEADER                                       │
├─────────────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐           │
│  │  Block      │  │  Creation   │  │  Compression│  │  Checksum   │           │
│  │  Metadata   │  │  Timestamp  │  │  Algorithm  │  │             │           │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘           │
└─────────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              SERIES INDEX                                       │
├─────────────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐           │
│  │  Series     │  │  Label      │  │  Sample     │  │  Offset     │           │
│  │  ID         │  │  Index      │  │  Count      │  │  Table      │           │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘           │
└─────────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              COMPRESSED DATA                                    │
├─────────────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐           │
│  │  Timestamp  │  │  Value      │  │  Label      │  │  Metadata   │           │
│  │  Data       │  │  Data       │  │  Data       │  │  Data       │           │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘           │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### **Block Lifecycle Management**

#### **Block Creation**
```
1. New Data Arrival
   ↓
2. Block Selection (Hot/Warm/Cold)
   ↓
3. Series Assignment
   ↓
4. Data Compression
   ↓
5. Index Creation
   ↓
6. Block Writing
   ↓
7. Cache Update
```

#### **Block Promotion/Demotion**
```
Hot Block (Frequently Accessed)
   ↓
Warm Block (Moderate Access)
   ↓
Cold Block (Rarely Accessed)
   ↓
Archive Block (Historical Data)
```

#### **Block Compaction**
```
Multiple Small Blocks
   ↓
Data Analysis
   ↓
Compression Optimization
   ↓
Block Merging
   ↓
Index Consolidation
   ↓
Single Large Block
```

## 🗜️ **Compression Architecture**

### **Multi-Algorithm Compression System**

#### **Compression Pipeline**
```
Raw Data
   ↓
Type Detection
   ↓
Algorithm Selection
   ↓
Compression Execution
   ↓
Compression Validation
   ↓
Compressed Data
```

#### **Compression Algorithms**

##### **1. Timestamp Compression**
```
Raw Timestamps
   ↓
Delta Calculation
   ↓
Delta-of-Delta Calculation
   ↓
Zigzag Encoding
   ↓
Variable-Length Encoding
   ↓
Compressed Timestamps
```

##### **2. Value Compression**
```
Raw Values
   ↓
Type Detection (Counter, Gauge, Histogram, Constant)
   ↓
Pattern Analysis
   ↓
Algorithm Selection:
   ├── Gorilla Compression (for time series)
   ├── Run-Length Encoding (for repeated values)
   ├── Dictionary Compression (for categorical data)
   └── Simple Compression (for constant values)
   ↓
Compressed Values
```

##### **3. Label Compression**
```
Raw Labels
   ↓
Dictionary Building
   ↓
String Deduplication
   ↓
Huffman Encoding
   ↓
Label Compression
   ↓
Compressed Labels
```

### **Adaptive Compression Engine**

#### **Compression Strategy Selection**
```
Data Analysis
   ├── Data Type Detection
   ├── Pattern Recognition
   ├── Entropy Calculation
   └── Compression Ratio Estimation
   ↓
Strategy Selection
   ├── High Compression (for cold data)
   ├── Balanced Compression (for warm data)
   └── Fast Compression (for hot data)
```

## 📊 **Cache Architecture**

### **Multi-Level Cache System**

#### **L1 Cache (Working Set Cache)**
```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              L1 CACHE (WORKING SET)                            │
├─────────────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐           │
│  │  LRU        │  │  Hit/Miss   │  │  Memory     │  │  Statistics │           │
│  │  Eviction   │  │  Tracking   │  │  Management │  │  Collection │           │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘           │
│                                                                                 │
│  • In-Memory Storage                                                           │
│  • Fastest Access (<0.1ms)                                                     │
│  • 98.52% Hit Ratio                                                            │
│  • Configurable Size                                                           │
└─────────────────────────────────────────────────────────────────────────────────┘
```

#### **L2 Cache (Memory Mapped Cache)**
```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              L2 CACHE (MEMORY MAPPED)                          │
├─────────────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐           │
│  │  Memory     │  │  File       │  │  Page       │  │  Cache      │           │
│  │  Mapping    │  │  Management │  │  Management │  │  Statistics │           │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘           │
│                                                                                 │
│  • Memory-Mapped Files                                                         │
│  • Medium Access Speed (1-5ms)                                                 │
│  • Larger Capacity                                                             │
│  • Persistent Storage                                                          │
└─────────────────────────────────────────────────────────────────────────────────┘
```

#### **L3 Cache (Predictive Cache)**
```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              L3 CACHE (PREDICTIVE)                             │
├─────────────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐           │
│  │  Pattern    │  │  Access     │  │  Prefetch   │  │  Confidence │           │
│  │  Detection  │  │  Tracking   │  │  Logic      │  │  Scoring    │           │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘           │
│                                                                                 │
│  • Pattern-Based Prefetching                                                   │
│  • Access Sequence Tracking                                                    │
│  • Confidence-Based Decisions                                                  │
│  • 15-25% Hit Ratio Improvement                                               │
└─────────────────────────────────────────────────────────────────────────────────┘
```

## 🔄 **Data Flow in Storage**

### **Write Flow**
```
1. Client Write Request
   ↓
2. Object Pooling (Memory Optimization)
   ↓
3. L1 Cache Update (Hot Data)
   ↓
4. Block Manager Selection
   ↓
5. Compression (Adaptive)
   ↓
6. Block Writing
   ↓
7. Index Update
   ↓
8. Background Processing (Compaction)
```

### **Read Flow**
```
1. Query Request
   ↓
2. L1 Cache Check
   ↓
3. L2 Cache Check (if L1 miss)
   ↓
4. L3 Cache Check (if L2 miss)
   ↓
5. Block Manager Lookup
   ↓
6. Block Reading
   ↓
7. Decompression
   ↓
8. Data Assembly
   ↓
9. Response Generation
```

## 📈 **Storage Performance Goals**

### **Throughput Metrics**
- **Write Throughput**: Optimized for high volume ingestion
- **Read Throughput**: Optimized for low latency queries
- **Compression Ratio**: 20-60% data reduction target

### **Storage Efficiency**
- **Memory Usage**: Minimal overhead per series
- **Compression Efficiency**: High storage reduction via tiered compression
- **Block Utilization**: High block fill rate via buffering

## 🔧 **Storage Configuration**

### **Block Configuration**
```yaml
block:
  max_size: 64MB
  max_records: 1000000
  duration: 2h
  compression: adaptive
  compaction_threshold: 0.8
```

### **Cache Configuration**
```yaml
cache:
  l1_size: 1GB
  l2_size: 10GB
  l3_size: 100GB
  eviction_policy: lru
  prefetch_enabled: true
```

### **Compression Configuration**
```yaml
compression:
  algorithm: adaptive
  level: balanced
  threshold: 0.1
  type_detection: true
```

## 🚀 **High-Concurrency Sharded Architecture**

### **Overview**
The storage engine uses fine-grained sharding for critical write-path components (WAL and Index) to eliminate global lock contention. This allows the system to scale writes linearly with thread count.

### **Architecture Components**

#### **Sharded Write Ahead Log (WAL)**
-   **16 Shards**: The WAL is partitioned into 16 independent shards.
-   **Async Batching**: Each shard uses a background thread to batch writes.
-   **Hash Distribution**: Series are assigned to shards based on a hash of their labels/ID.
-   **Independent Locking**: Each shard has its own mutex and file writer.
-   **Benefit**: Reduces lock contention and decouples client latency from disk I/O.
-   **Replay Safety**:
    -   **Init Phase**: `replay_at_init()` runs lock-free to prevent deadlocks during single-threaded startup.
    -   **Runtime Phase**: `replay_runtime()` acquires locks to prevent data races during hot restore.

#### **Sharded Index**
-   **16 Shards**: The In-Memory Index is partitioned into 16 shards.
-   **Shared Mutexes**: Each shard uses `std::shared_mutex` allowing multiple concurrent readers.
-   **Scatter-Gather Lookups**: Search operations query all shards in parallel.
-   **Benefit**: Enables high-throughput concurrent series creation and lookups.

### **Performance Characteristics**

#### **Performance Characteristics**
- **Throughput**: Significantly improved via async I/O
- **Latency**: Decouples client write latency from disk I/O latency

### **Configuration**
The number of shards is currently fixed at compile time (default 16) but designed to be configurable.

## 🚀 **Index Optimization Architecture**

The Index layer is the critical path for query performance. We employ multiple optimization techniques to achieve sub-millisecond lookups for typical queries.

### **Roaring Bitmap Index**

#### **Overview**
Traditional TSDB indices use sorted arrays for posting lists. We replace these with **Roaring Bitmaps** (CRoaring v4.5.0) for **10-50x faster** set operations.

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                         ROARING BITMAP INDEX STRUCTURE                          │
└─────────────────────────────────────────────────────────────────────────────────┘

    Label Pair                    Posting List (Roaring64Map)
    ─────────────                 ─────────────────────────────────
    
    namespace=prod  ────────────► [1001, 1002, 1003, ..., 50000]
                                     ▲
                                     │  Roaring Bitmap: Compressed
                                     │  using run-length encoding,
                                     │  bit arrays, or sorted arrays
                                     │  based on density
    
    pod=nginx-abc   ────────────► [1001, 2001, 3001, ..., 5000]
    
    container=web   ────────────► [1001, 1002, 2001, 2002, ...]

                                    │
                                    ▼ AND operation (Intersection)
                                    
    RESULT: {namespace=prod, pod=nginx-abc, container=web}
            = [1001, ...]  ◄──────── Single matching series in <1μs
```

#### **Data Structures**

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              POSTING LIST INTERNALS                             │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│  Roaring Bitmap Container Types (chosen automatically per 65536-value chunk):  │
│                                                                                 │
│  ┌─────────────────────┐  ┌─────────────────────┐  ┌─────────────────────┐    │
│  │   Array Container   │  │   Bitmap Container  │  │    Run Container    │    │
│  ├─────────────────────┤  ├─────────────────────┤  ├─────────────────────┤    │
│  │ Sorted uint16 array │  │ 8KB bit array       │  │ Run-length encoded  │    │
│  │ Used when <4096 vals│  │ Used when ≥4096 vals│  │ Used for sequences  │    │
│  │                     │  │                     │  │                     │    │
│  │ [1, 5, 10, 20, 100] │  │ 10101...01010       │  │ [(1,50), (100,200)] │    │
│  └─────────────────────┘  └─────────────────────┘  └─────────────────────┘    │
│                                                                                 │
│  Roaring64Map: Splits 64-bit IDs into (high 32-bit key, low 32-bit value)      │
│  • High bits = map key                                                         │
│  • Low bits = stored in 32-bit Roaring bitmap                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

#### **Query Flow with Roaring Bitmaps**

```
                           LABEL MATCHER QUERY: {app="nginx", env="prod"}
                                            │
                                            ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│  Step 1: Parallel Shard Query                                                   │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│   ┌────────┐  ┌────────┐  ┌────────┐       ┌────────┐                         │
│   │Shard 0 │  │Shard 1 │  │Shard 2 │  ...  │Shard 15│                         │
│   │        │  │        │  │        │       │        │                         │
│   │app=nginx│  │app=nginx│  │app=nginx│       │app=nginx│                         │
│   │  ∩      │  │  ∩      │  │  ∩      │       │  ∩      │                         │
│   │env=prod │  │env=prod │  │env=prod │       │env=prod │                         │
│   └────┬───┘  └────┬───┘  └────┬───┘       └────┬───┘                         │
│        │           │           │                │                              │
│        ▼           ▼           ▼                ▼                              │
│    [1001,1005]  [2002,2010] [3001,3015]     [16001]                           │
│                                                                                 │
│   TBB parallel_for: Each shard executes Roaring bitmap AND concurrently        │
└─────────────────────────────────────────────────────────────────────────────────┘
                                            │
                                            ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│  Step 2: Result Aggregation                                                     │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│   Combined Result = [1001, 1005, 2002, 2010, 3001, 3015, 16001]                │
│                                                                                 │
│   • Zero-copy: Roaring iterators stream directly to result vector              │
│   • Pre-allocated: Result vector sized to sum of shard cardinalities           │
└─────────────────────────────────────────────────────────────────────────────────┘
```

#### **Performance Metrics**

| Operation               | std::vector     | Roaring Bitmap  | Speedup    |
|------------------------|-----------------|-----------------|------------|
| Intersection (10K×10K) | ~500μs          | ~10μs           | **50x**    |
| Union (10K×10K)        | ~800μs          | ~15μs           | **53x**    |
| Memory (1M series)     | ~8MB            | ~1-2MB          | **4-8x**   |
| Cardinality Count      | O(n)            | O(1)*           | **1000x+** |

*\* Roaring bitmaps maintain cardinality as metadata*

### **Secondary Index for Cold Tier (Parquet)**

The Cold Tier uses Apache Parquet for storage. To avoid scanning all row groups, we maintain a **B+ Tree Secondary Index** that maps time ranges and series IDs to specific row groups.

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                    PARQUET SECONDARY INDEX ARCHITECTURE                         │
└─────────────────────────────────────────────────────────────────────────────────┘

                          Query: series_id=1001, time=[T1, T2]
                                          │
                                          ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           B+ TREE SECONDARY INDEX                               │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│                              ┌──────────────┐                                  │
│                              │  Root Node   │                                  │
│                              │  [T100, T500]│                                  │
│                              └──────┬───────┘                                  │
│                     ┌───────────────┼───────────────┐                          │
│                     ▼               ▼               ▼                          │
│              ┌──────────┐   ┌──────────┐   ┌──────────┐                        │
│              │ [T1-T100]│   │[T101-T500]│   │[T501-T1000]│                      │
│              └────┬─────┘   └────┬─────┘   └────┬─────┘                        │
│                   │              │              │                              │
│                   ▼              ▼              ▼                              │
│           ┌────────────┐ ┌────────────┐ ┌────────────┐                        │
│           │ Leaf: RG0  │ │ Leaf: RG1  │ │ Leaf: RG2  │                        │
│           │ series: 1-1K│ │ series: 1-1K│ │ series: 1-1K│                      │
│           │ bloom_filter│ │ bloom_filter│ │ bloom_filter│                      │
│           └────────────┘ └────────────┘ └────────────┘                        │
│                                                                                 │
│  Index Entry:                                                                   │
│  ┌──────────────────────────────────────────────────────────────────────────┐ │
│  │ file_path | row_group_id | min_time | max_time | series_bloom | checksum │ │
│  └──────────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────────┘
                                          │
                                          ▼
                            Row Groups RG0, RG1 identified
                            (RG2 skipped: time range doesn't match)
```

### **Bloom Filters for Series Filtering**

Each Parquet row group has an associated **Bloom Filter** to quickly determine if a series ID exists without reading the row group.

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           BLOOM FILTER SKIP LOGIC                               │
└─────────────────────────────────────────────────────────────────────────────────┘

    Query: Find series_id=42 in row_group RG0
                    │
                    ▼
    ┌───────────────────────────────────────┐
    │  Bloom Filter Check: hash(42)         │
    │  ┌─────────────────────────────────┐  │
    │  │ 0 1 1 0 0 1 0 1 0 0 1 0 1 0 0 1 │  │  ◄── Bit array (e.g., 1024 bits)
    │  └─────────────────────────────────┘  │
    │           ↑   ↑       ↑               │
    │         h1(42) h2(42) h3(42)          │  ◄── Multiple hash functions
    └────────────────────┬──────────────────┘
                         │
           ┌─────────────┴─────────────┐
           │                           │
           ▼                           ▼
    ┌─────────────┐            ┌─────────────┐
    │ All bits=1  │            │ Any bit=0   │
    │ MAYBE EXISTS│            │ DEFINITELY  │
    │             │            │ NOT EXISTS  │
    │ Read RG0    │            │ SKIP RG0    │
    └─────────────┘            └─────────────┘

    False Positive Rate: ~1% (tunable via filter size)
    Space: ~10 bits per series ID
    Lookup: O(k) where k = number of hash functions (typically 3-7)
```

### **Combined Index Query Optimization**

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│               FULL COLD TIER QUERY OPTIMIZATION PIPELINE                        │
└─────────────────────────────────────────────────────────────────────────────────┘

   PromQL: rate(http_requests_total{namespace="prod"}[5m])
                               │
                               ▼
   ┌─────────────────────────────────────────────────────────────────────────────┐
   │ Step 1: HOT TIER INDEX (Roaring Bitmap)                                     │
   │         Resolve {namespace="prod"} → [1001, 1002, ..., 5000] in ~50μs       │
   └───────────────────────────────────────┬─────────────────────────────────────┘
                               │
                               ▼
   ┌─────────────────────────────────────────────────────────────────────────────┐
   │ Step 2: SECONDARY INDEX LOOKUP                                              │
   │         Time range [now-5m, now] → Candidate Parquet files & row groups     │
   │         Result: [file1.parquet:RG0, file1.parquet:RG1, file2.parquet:RG0]   │
   └───────────────────────────────────────┬─────────────────────────────────────┘
                               │
                               ▼
   ┌─────────────────────────────────────────────────────────────────────────────┐
   │ Step 3: BLOOM FILTER PRUNING                                                │
   │         For each series_id in [1001, 1002, ...]:                            │
   │           • Check bloom filter of each candidate row group                  │
   │           • Skip row groups where series definitely not present             │
   │         Result: Pruned to [file1.parquet:RG0, file2.parquet:RG0]            │
   │         (file1.parquet:RG1 skipped - bloom filter negative)                 │
   └───────────────────────────────────────┬─────────────────────────────────────┘
                               │
                               ▼
   ┌─────────────────────────────────────────────────────────────────────────────┐
   │ Step 4: PARQUET COLUMNAR READ                                               │
   │         • Read only timestamp and value columns                             │
   │         • Use row group statistics for additional predicate pushdown        │
   │         • Decompress with ZSTD (dictionary-encoded labels)                  │
   └───────────────────────────────────────┬─────────────────────────────────────┘
                               │
                               ▼
                         Query Result

   OPTIMIZATION SUMMARY:
   ┌────────────────────────────────────────────────────────────────────────────┐
   │ Without Optimization │ With Optimization │ Data Reduction                  │
   ├──────────────────────┼───────────────────┼─────────────────────────────────┤
   │ Scan all Parquet     │ Read 2 row groups │ 90-99% I/O eliminated           │
   │ files (100GB+)       │ (~200MB)          │                                 │
   │                      │                   │                                 │
   │ Read all columns     │ Read 2 columns    │ 60-80% data skipped             │
   │                      │                   │                                 │
   │ Decompress all       │ Decompress        │ 95%+ decompression avoided      │
   │ series               │ matching series   │                                 │
   └──────────────────────┴───────────────────┴─────────────────────────────────┘
```

## 🔍 **Storage Monitoring**

### **Performance Metrics**
- **Block Creation Rate**: blocks/sec
- **Compression Ratio**: percentage
- **Cache Hit Ratio**: percentage
- **I/O Throughput**: MB/sec
- **Latency Percentiles**: p50, p95, p99

### **Resource Metrics**
- **Memory Usage**: MB/GB
- **Disk Usage**: GB/TB
- **CPU Utilization**: percentage
- **I/O Wait**: percentage

### **Operational Metrics**
- **Block Count**: total blocks
- **Series Count**: active series
- **Compaction Rate**: compactions/sec
- **Error Rate**: errors/sec

---

*This storage architecture provides a comprehensive multi-tier storage system with intelligent caching, adaptive compression, and efficient block management, delivering high performance and optimal resource utilization for time series data.* 