# Performance Tests for OTEL/gRPC
# These tests benchmark OTEL/gRPC write performance

if(HAVE_GRPC)
    # Note: Performance tests for OTEL/gRPC will be added in Phase 2
    # This directory is for benchmarking:
    # - Write throughput via OTEL/gRPC
    # - Latency measurements
    # - Batch processing performance
    # - Concurrent write performance
    
    message(STATUS "OTEL/gRPC performance tests directory ready")
    
    # Performance tests will be added in Phase 2 of implementation
endif()

